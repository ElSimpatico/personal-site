import { h } from '@stencil/core';
import { SpecPage, newSpecPage } from '@stencil/core/testing';
import { {{pascalCase name}} } from './{{dashCase name}}';

let page: SpecPage;
let {{camelCase name}}: HTMLUi{{pascalCase name}}Element;

beforeEach(async () => {
    page = await newSpecPage({
        components: [{{pascalCase name}}],
        template: () => <ui-{{dashCase name}}></ui-{{dashCase name}}>,
    });
    {{camelCase name}} = page.root as HTMLUi{{pascalCase name}}Element;
});

it('should render {{lowerCase name}}', async () => {
    expect({{camelCase name}}).toEqualHtml(`
    <ui-{{dashCase name}}><div>Add your html</div></ui-{{dashCase name}}>
  `);
});
